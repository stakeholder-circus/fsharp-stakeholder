# Parity

## Role

Functional .NET parity sibling.

## Parity class

- full-parity

## Method and Review Model

- Codex-assisted
- Manually reviewed
- Human in the loop
- Derived from `giacomo-b/rust-stakeholder` where applicable
- Missing behavior must fail fast and be recorded explicitly in `GAPS.md`

## Current slice

- Deterministic event generation is implemented for the shared CLI contract.
- The full family registry is exposed via `--list-values`.
- Dedicated renderer depth is implemented across all classic-six families.
- Dedicated renderer depth is implemented across all modern-core families.
- Later tranches stay on explicit grouped fallback and are tracked in `GAPS.md`.
- The local first-push bar is met through the Docker-backed .NET 8 validation lane on this workstation.

## Publication blockers

- Remote creation and first push are intentionally deferred by policy.
- Exact GitHub required-check binding must wait for the first stable CI pass after publication.
- `flake.lock` remains pending until `nix` is available locally.

## Promotion after this tranche

- Keep deterministic normalized JSON and the shared CLI contract from `stakeholder-core` intact.
- Preserve explicit provenance and conservative MIT attribution as the repo widens beyond modern-core.
- Use this repo as the first wider-matrix template for Zig and the rest of the queue only after the docs and validation surfaces stay synchronized.
